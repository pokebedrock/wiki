---
title: Primeros pasos
description: Configura el repo de la wiki de PokéBedrock localmente y publica tu primer cambio de docs.
tags:
  - guide
  - onboarding
lastUpdated: "2026-04-13"
status: stable
lang: es
toc: true
order: 1
---

<InfoCallout />

## Requisitos previos

- Node.js 20.0.0 o superior (igual que el campo `"engines"` en `package.json`)
- npm 10.x (incluido con Node 20)

Verifica tus versiones:

<ShellBlock command="node --version\nnpm --version" />

Si usas `nvm`, instala y activa el runtime correcto antes de ejecutar comandos del repo:

<ShellBlock command="nvm install 20\nnvm use 20" />

## 1. Clonar e instalar

<ShellBlock command="git clone git@github.com:pokebedrock/wiki.git\ncd wiki\nnpm ci" />

## 2. Crear una rama

<ShellBlock command="git checkout -b docs/my-topic" />


## 3. Crear contenido

- Elegi una categoria bajo `docs/`.
- Copia la plantilla de frontmatter desde `CONTRIBUTING.md`.
- Agrega assets con ruta relativa al documento (por ejemplo,
  `../../assets/images/ui.webp`).

Usa MDX si necesitas componentes; en caso contrario, usa Markdown por legibilidad.

## 4. Validar

<ShellBlock command="npm run ci" />

## 5. Abrir una PR

Completa la plantilla de PR, enlaza issues relacionados y solicita revision a `@pokebedrock/wiki-maintainers`.

