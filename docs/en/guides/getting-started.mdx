---
title: Getting Started
description: Set up the PokéBedrock wiki repo locally and publish your first doc change.
tags:
  - guide
  - onboarding
lastUpdated: "2025-11-21"
status: stable
lang: en
toc: true
order: 1
---

<InfoCallout />

## 1. Clone & Install

<ShellBlock command="git clone git@github.com:pokebedrock/wiki.git\ncd wiki\nnpm ci" />

## 2. Create a Branch

<ShellBlock command="git checkout -b docs/my-topic" />


## 3. Author Content

- Pick a category under `docs/`.
- Copy the frontmatter template from `CONTRIBUTING.md`.
- Add assets relative to the document path (for example,
  `../../assets/images/ui.webp`).

Use MDX if you need components, otherwise stick to Markdown for readability.

## 4. Validate

<ShellBlock command="npm run ci\nnpm run audit:prod" />

`npm run ci` covers linting, link checks, and generated search/manifests drift. Run
`npm run audit:prod` as well before opening a PR so your branch matches the same
production dependency audit that GitHub Actions enforces on pushes and pull requests.

## 5. Open a PR

Fill out the PR template, link related issues, and request a review from `@pokebedrock/wiki-maintainers`.



