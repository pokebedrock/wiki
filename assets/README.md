# Assets

- Place optimized `webp` or `svg` files under `images/`.
- Store editable sources (`.drawio`, `.excalidraw`) under `diagrams/`.
- Track binaries larger than 1 MB with Git LFS.
- Use relative paths when referencing assets from docs (e.g. `![UI](../../assets/images/ui.webp)`).


